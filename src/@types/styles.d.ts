export interface styleGuide {
  colors: {
    primary: colors;
    white: colors;
    black: colors;
    blue: {
      ocean: colors;
    };
    gray: {
      100: colors;
      300: colors;
    };
  };
}
